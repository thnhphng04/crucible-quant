# Paper PDFs — kept locally, linked from the docs instead (licensing + size)
*.pdf
*.mq5

# Local tool state
.omx/
# Claude Code: share project settings, hooks and commands; keep personal settings local
.claude/*
!.claude/settings.json
!.claude/hooks/
!.claude/commands/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.env
*.egg-info/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.import_linter_cache/

# Data, results, holdout — never committed (root-anchored: src/quantcrucible/data/ is code)
/data/
/results/
/holdout/
/ledger/*.db*
/ledger/*.runlock
*.sqlite

# Generated per campaign from config/user.yaml (§10.1) — never hand-edited, never committed
config/evaluation.lock.yaml
config/locks/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Review UI: installed packages, build output and tsc incremental state
ui/node_modules/
ui/dist/
ui/test-results/
ui/playwright-report/
ui/screenshots/
*.tsbuildinfo
ui/vite.config.js
ui/vite.config.d.ts
ui/playwright.config.js
ui/playwright.config.d.ts
